<%- include('partials/header') %>
<%- include('partials/navigation') %>

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <!-- Quick Actions -->
      <div class="mb-8 bg-gray-800 dark:bg-gray-800 rounded-lg shadow p-6">
        <h2 class="text-lg font-medium text-white mb-4">Actions Rapides</h2>
        <div class="flex flex-wrap gap-3">
          <button class="inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors duration-200"
                  hx-get="/planning"
                  hx-trigger="click"
                  hx-swap="outerHTML"
                  hx-target="body">
            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
            </svg>
            Actualiser le Planning
          </button>
        </div>
      </div>

      <!-- Planning Grid -->
      <%- include('partials/week-planning-grid') %>

      <!-- Legend -->
      <div class="mt-6 bg-gray-800 dark:bg-gray-800 rounded-lg shadow p-6">
        <h3 class="text-lg font-medium text-white mb-4">Légende</h3>
        <div class="flex flex-wrap gap-6">
          <div class="flex items-center">
            <span class="inline-block w-4 h-4 bg-green-500 rounded-full mr-2"></span>
            <span class="text-sm text-gray-300">Ouverture - Minimum 1 membre requis</span>
          </div>
          <div class="flex items-center">
            <span class="inline-block w-4 h-4 bg-red-500 rounded-full mr-2"></span>
            <span class="text-sm text-gray-300">Fermeture - Minimum 2 membres requis</span>
          </div>
        </div>
      </div>
    </main>

<%- include('partials/footer') %>